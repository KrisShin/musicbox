import React from 'react';
import { Empty } from 'antd';

const PlaylistPage: React.FC = () => {
    return (
        <div style={{ padding: '50px', textAlign: 'center' }}>
            <Empty description="歌单功能正在开发中..." />
        </div>
    );
};

export default PlaylistPage;