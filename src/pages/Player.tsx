import React from 'react';
import { Empty } from 'antd';

const PlayerPage: React.FC = () => {
  return (
    <div style={{ padding: '50px', textAlign: 'center' }}>
      <Empty description="播放器详情页正在开发中..." />
    </div>
  );
};

export default PlayerPage;